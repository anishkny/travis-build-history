{
  "scripts": {
    "test": "mocha",
    "build": "rm -rf dist; cp -r src dist; netlify-lambda build dist"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "netlify-lambda": "^0.4.0",
    "sinon": "^6.3.5"
  }
}
